#include "main.h"
#include <unistd.h>

/**
 * _putchar - writes the characer c to stdout
 * @c: the character to print
 *
 * Return: on success
 */
int _putchar(char C)
{
	return (write(1, $c, 1));
}
